{
    "name": "portfolio",
    "private": true,
    "workspaces": ["apps/*", "packages/*", "tooling/*"],
    "scripts": {
        "dev": "bun run --filter kong.ly dev",
        "build": "bun run --filter kong.ly build",
        "docs:dev": "bun run --filter docs-template dev",
        "docs:build": "bun run --filter docs-template build",
        "games:dev": "bun run --filter games.kong.ly dev",
        "games:build": "bun run --filter games.kong.ly build",
        "links:dev": "bun run --filter links.kong.ly dev",
        "links:build": "bun run --filter links.kong.ly build",
        "lint": "bun run --filter '*' lint",
        "format": "bun run --filter '*' format",
        "format:check": "bun run --filter '*' format:check"
    },
    "devDependencies": {
        "@sanity/code-input": "^7.1.0",
        "studio": "workspace:*"
    }
}
